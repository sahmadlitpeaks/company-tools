# Claude Repository Instructions

@AGENTS.md

Treat `AGENTS.md` as the canonical repository instructions. For frontend work,
also read `docs/FRONTEND_COMPONENTS.md` and use the repository skills under
`.claude/skills/`, which redirect to their canonical definitions under
`.agents/skills/`.
