# One-command, single-origin stack.
#
#   docker compose up --build
#
# Everything is served from ONE URL (http://localhost:8080 by default): the
# frontend's nginx serves the built SPA and reverse-proxies /api, /s, /media,
# /docs and /health to the backend. Because the frontend is built with an empty
# VITE_API_BASE_URL it uses relative URLs, so there is no CORS and the whole
# thing is host-agnostic. Data persists in the named volumes (pgdata + media).
#
# No .env editing is required to boot locally; override any value via the shell
# or an .env file in this directory (see .env.example).
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-platform}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-platform}
      POSTGRES_DB: ${POSTGRES_DB:-platform}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-platform}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      args:
        SHAREPOINT_NLP: ${SHAREPOINT_ENABLED:-false}
    # Exposed for local Vite (`npm run dev` on :5173 proxies here). Production
    # still goes through the frontend nginx single-origin setup on APP_PORT.
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-platform}:${POSTGRES_PASSWORD:-platform}@db:5432/${POSTGRES_DB:-platform}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      SECRET_KEY: ${SECRET_KEY:-change-me-to-a-long-random-string}
      DEFAULT_ADMIN_EMAIL: ${DEFAULT_ADMIN_EMAIL:-admin@agholding.net}
      DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD:-admin}
      RUN_SCHEDULER: ${RUN_SCHEDULER:-true}
      # Blank => auto-derive public links/redirects from the request host, so
      # the app works on any IP/domain with no config. Set them for a fixed
      # domain or to force https behind TLS termination.
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-}
      FRONTEND_BASE_URL: ${FRONTEND_BASE_URL:-}
      BACKEND_CORS_ORIGINS: ${BACKEND_CORS_ORIGINS:-http://localhost:8080,http://localhost:5173,http://127.0.0.1:5173}
      MEDIA_ROOT: /data/media
      BACKUP_ROOT: /data/backups
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-30}
      BACKUP_HOUR_DUBAI: ${BACKUP_HOUR_DUBAI:-2}
      BACKUP_IMPORT_MAX_BYTES: ${BACKUP_IMPORT_MAX_BYTES:-5368709120}
      AI_BASE_URL: ${AI_BASE_URL:-}
      AI_API_KEY: ${AI_API_KEY:-}
      AI_MODEL: ${AI_MODEL:-}
      SHAREPOINT_ENABLED: ${SHAREPOINT_ENABLED:-false}
      SHAREPOINT_TENANT_ID: ${SHAREPOINT_TENANT_ID:-}
      SHAREPOINT_CLIENT_ID: ${SHAREPOINT_CLIENT_ID:-}
      SHAREPOINT_CLIENT_SECRET: ${SHAREPOINT_CLIENT_SECRET:-}
      SHAREPOINT_SITE_ID: ${SHAREPOINT_SITE_ID:-}
      SHAREPOINT_DRIVE_ID: ${SHAREPOINT_DRIVE_ID:-}
      SHAREPOINT_FOLDER_ID: ${SHAREPOINT_FOLDER_ID:-}
      SHAREPOINT_REDIRECT_URI: ${SHAREPOINT_REDIRECT_URI:-${PUBLIC_BASE_URL:-http://localhost:8080}/api/sharepoint/callback}
      SHAREPOINT_ENCRYPTION_KEY: ${SHAREPOINT_ENCRYPTION_KEY:-}
      SHAREPOINT_PREVIOUS_ENCRYPTION_KEYS: ${SHAREPOINT_PREVIOUS_ENCRYPTION_KEYS:-}
      SHAREPOINT_OPENAI_API_KEY: ${SHAREPOINT_OPENAI_API_KEY:-}
      SHAREPOINT_OPENAI_MODEL: ${SHAREPOINT_OPENAI_MODEL:-}
      SHAREPOINT_REVIEWER_IDS: ${SHAREPOINT_REVIEWER_IDS:-}
      SHAREPOINT_POLLING_ENABLED: ${SHAREPOINT_POLLING_ENABLED:-true}
      SHAREPOINT_SYNC_INTERVAL_SECONDS: ${SHAREPOINT_SYNC_INTERVAL_SECONDS:-60}
      SHAREPOINT_MAX_FILE_BYTES: ${SHAREPOINT_MAX_FILE_BYTES:-26214400}
      SHAREPOINT_MAX_TEXT_CHARS: ${SHAREPOINT_MAX_TEXT_CHARS:-200000}
      SHAREPOINT_MAX_ITEMS: ${SHAREPOINT_MAX_ITEMS:-2000}
      SHAREPOINT_NER_LANGUAGES: ${SHAREPOINT_NER_LANGUAGES:-ar,en}
      SHAREPOINT_NER_MODEL_DIR: /data/nlp-models
      SHAREPOINT_PARSER_TIMEOUT_SECONDS: ${SHAREPOINT_PARSER_TIMEOUT_SECONDS:-120}
      AZURE_TENANT_ID: ${AZURE_TENANT_ID:-}
      AZURE_CLIENT_ID: ${AZURE_CLIENT_ID:-}
      AZURE_CLIENT_SECRET: ${AZURE_CLIENT_SECRET:-}
      AZURE_REDIRECT_URI: ${AZURE_REDIRECT_URI:-${PUBLIC_BASE_URL:-http://localhost:8080}/api/auth/callback}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_STARTTLS: ${SMTP_STARTTLS:-true}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-no-reply@agholding.net}
      TEAMS_WEBHOOK_URL: ${TEAMS_WEBHOOK_URL:-}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - media:/data/media
      - backups:/data/backups
      - sharepoint_nlp:/data/nlp-models
    command: >
      sh -c "alembic upgrade head &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000"

  frontend:
    build:
      context: ./frontend
      args:
        # Empty base URL => the SPA uses relative URLs, which nginx proxies to
        # the backend. This is what makes the single-origin setup work.
        VITE_API_BASE_URL: ""
    depends_on:
      - backend
    ports:
      - "${APP_PORT:-8080}:80"

volumes:
  pgdata:
  media:
  backups:
  sharepoint_nlp:
