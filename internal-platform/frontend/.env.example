# Base URL of the FastAPI backend
VITE_API_BASE_URL=http://localhost:8000

# Azure values are only needed if you switch to client-side MSAL login.
# The default flow is backend-driven OIDC, so these are optional.
VITE_AZURE_CLIENT_ID=
VITE_AZURE_TENANT_ID=
